name: proxy_metricx
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - pip
  - pip:
      # Core MetricX and MT Evaluation
      - transformers==4.30.2
      - torch==2.7.1
      - sentencepiece==0.1.99

      # Data Handling
      - pandas==2.3.3
      - numpy==2.0.2
      - datasets==4.1.1

      # Utilities
      - accelerate==1.10.1
      - huggingface-hub==0.35.3
      - tqdm==4.67.1
      - scipy==1.13.1
      - pyyaml==6.0.3
      - "protobuf<4.0.0"
